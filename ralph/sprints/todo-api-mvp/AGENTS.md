# AGENTS.md (sprint-local)

このファイルは、このスプリント固有の前提・規約を書く場所です。
ルートの `AGENTS.md` と重複する内容は書かず、ここでしか効かないローカルルール
（例: このスプリント特有の DB スキーマ規約、API レスポンス規約、外部依存の使い方）
だけを書いてください。

CLAUDE.md は `@AGENTS.md` のブリッジです（Claude Code 用）。

---

<!-- ここに書く例:

## このスプリント固有のルール
- すべての API レスポンスは ISO 8601 (UTC) の日時を返す
- DB マイグレーションは必ず Alembic で行う（生 SQL は書かない）
- 新しい依存を追加する場合は decisions.md に理由を書いてから入れる

-->
